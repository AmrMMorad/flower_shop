require "spec_helper"

RSpec.describe FlowerShop do
  it "has a version number" do
    expect(FlowerShop::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
